# roverd-android

Android rover client for MultiRoombaRover.

This repository is being initialized as a dedicated-device Android client that connects directly to a Roomba over USB serial and to the existing MultiRoombaRover server over WebSocket.

Initial milestone: reliable USB serial, BRC pulsing via RTS/DTR, sensor forwarding, drive commands, command acknowledgements, and automatic reconnect.
